import Link from "next/link";
import { requireAuthUser as requireChatGPTUser } from "@/app/auth";
import { listAnalyses } from "@/src/infrastructure/storage/repository";

export const dynamic = "force-dynamic";
export default async function TendersPage() { const user = await requireChatGPTUser("/dashboard/tenders"); const analyses = await listAnalyses(user.userId, 50); return <><div className="dashboard-heading"><div><span className="section-kicker">Мої перевірки</span><h1>Журнал рішень.</h1><p>Зафіксовані оцінки з датою, режимом і версією даних.</p></div><Link href="/analyze" className="button button--primary">Новий аналіз</Link></div><section className="dashboard-card dashboard-card--table">{analyses.length ? <div className="dashboard-table dashboard-table--wide">{analyses.map((item) => <Link href={`/analyze?source=${encodeURIComponent(item.tenderExternalId)}`} key={item.id}><span className={`score-chip score-chip--${item.verdict}`}>{item.score}</span><span><b>{item.title}</b><small>{item.tenderExternalId} · {item.buyer}</small></span><i>{item.mode === "ai-enhanced" ? "AI + PDF" : "Структурований"}</i><time>{new Intl.DateTimeFormat("uk-UA").format(new Date(item.createdAt))}</time></Link>)}</div> : <div className="empty-state"><h3>Перевірок ще немає</h3><Link href="/analyze" className="button button--dark">Почати</Link></div>}</section></>; }
