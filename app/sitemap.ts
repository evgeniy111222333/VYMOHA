import type { MetadataRoute } from "next"; import { guides } from "@/src/content/guides";
export default function sitemap(): MetadataRoute.Sitemap { const base = "https://vymoha.app"; return [{ url: base, priority: 1, changeFrequency: "weekly" }, { url: `${base}/analyze`, priority: 0.9, changeFrequency: "weekly" }, { url: `${base}/guides`, priority: 0.8, changeFrequency: "weekly" }, ...guides.map((guide) => ({ url: `${base}/guides/${guide.slug}`, priority: 0.7, changeFrequency: "monthly" as const })), { url: `${base}/privacy`, priority: 0.2, changeFrequency: "yearly" }, { url: `${base}/terms`, priority: 0.2, changeFrequency: "yearly" }]; }
